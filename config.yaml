server:
  port: "8080"
  host: "localhost"
  read_timeout: 30
  write_timeout: 30

database:
  type: "mysql"  # or "postgres"
  host: "localhost"
  port: 3306
  username: "root"
  password: "password"
  database: "log_analyzer"
  ssl_mode: "disable"

logging:
  level: "info"
  output_file: "logs/app.log"
  max_size: 100
  max_backups: 3
